# Relay macOS icon

Generated with the built-in image_gen tool using `Assets/Relay-preview.png` as the identity reference, followed by a refinement pass. Final source: `Relay-modern-v1.png`, 1254 × 1254 RGBA (the requested 2048 size was not returned). The macOS export script produces the complete iconset through 1024 pixels. The original Windows artwork is preserved.

Final refinement prompt:

> Refine this Relay macOS app icon into a much more restrained and precise final asset. Preserve the custom R and separate dot shapes and mint-on-dark-teal identity. Remove the bright cyan glow and rough sparkling fringe entirely. Tile edge must be an exceptionally clean smooth continuous curve with one very subtle charcoal highlight, no luminous outline. Satin mint glyph, restrained soft bevel, less glossy than reference. Nearly uniform deep charcoal teal tile with a gentle smooth upper-left lighting gradient, no mottling. Front view, perfectly square canvas. Scale down the entire tile so its bounds run from 10% to 90% on both axes, leaving generous even transparent padding on every side. Transparent background, crisp antialiasing with zero stray pixels outside the faint tight shadow. Production macOS app icon, single asset, 2048x2048 if supported. No words or mockup or added objects.
