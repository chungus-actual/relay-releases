Long song titles now scroll within the media controls instead of truncating. Scrolling pauses at each end, resets when the track changes, and respects Reduce Motion. Short titles stay still.

This macOS preview is self-signed and is not notarized by Apple. Requires macOS 14 or later, on Apple silicon or Intel.
